<%
	var mdpath = typeof path != "undefined" ? path.replace(".html", ".md") : "blog/post-1.md";
	let { data, content } = readMarkdown(mdpath);
%>
<%- require("/views/blog-post.ejs")({
	site: readJson("site.json"),
	page: {
		title: "Blog Post",
		path
	},
	header: {...readJson("header.json"), current_page: page},
	content,
	...data
})
%>