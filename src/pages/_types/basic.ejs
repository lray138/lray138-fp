<%
	let { data, content } = readMarkdown(markdown_path);
%>
<%- require("/views/main.ejs")({
	site: readJson("site.json"),
	page: {
		title: "Blog Post",
		path
	},
	header: {...readJson("header.json"), current_page: page, name: site.name + "asdf"},
	content,
	data
});
%>