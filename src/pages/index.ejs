<%
	let { data, content } = readMarkdown("home.md");
	// Convert Markdown to HTML
	// dconst content = marked(markdownContent);
%>
<%- require("/templates/main.ejs")({
	site: readJson("site.json"),
	page: {
		title: "Blog Post",
		path
	},
	header: {...readJson("header.json"), current_page: page},
	content,
	data
})
%>