import 'bootstrap';

// HTMX
import 'htmx.org';
window.htmx = require('htmx.org');

// Ramda (if we want to utilize "surreal" and "ramda" we need it in the head)
// import * as R from "ramda"
// R.identity();

import "./flatpickr.js";
import "./dateTimeTextInput.js";

